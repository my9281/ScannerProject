<Window x:Class="Scanner.WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="{DynamicResource Logo}" Title="{DynamicResource LoginTitle}"
        Width="500" Height="700" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#EAF3F7" FontFamily="{DynamicResource AppFontFamily}" Loaded="Window_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2457D6"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#082F4D"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#315D7A"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#DDE4F0"/>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="46"/><Setter Property="Padding" Value="13,0"/><Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#C8D5EE"/><Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="46"/><Setter Property="FontSize" Value="16"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="9">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88"/></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72"/></Trigger><Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/></Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="24">
        <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="38,24">
            <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.09" Color="#15223A"/></Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <Border Width="40" Height="40" Background="#D8EAF2" CornerRadius="14" HorizontalAlignment="Center">
                        <TextBlock Text="S" Foreground="{StaticResource PrimaryBrush}" FontSize="27" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{DynamicResource LoginHeading}" FontSize="30" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    <TextBlock Text="{DynamicResource LoginTagline}" FontSize="15" FontWeight="Bold" Foreground="#315D7A" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0" TextElement.FontFamily="{DynamicResource AppFontFamily}">
                        <Button Content="中文" Tag="zh-CN" Padding="10,3" Click="LanguageButton_Click"/>
                        <Button Content="EN" Tag="en-US" Padding="10,3" Margin="6,0" Click="LanguageButton_Click"/>
                        <Button Content="ES" Tag="es-ES" Padding="10,3" Click="LanguageButton_Click"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Grid.Row="1" Text="{DynamicResource Username}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,7"/>
                <TextBox x:Name="UsernameTextBox" Grid.Row="2" Style="{StaticResource InputStyle}" Margin="0,0,0,12" KeyDown="Input_KeyDown"/>
                <TextBlock Grid.Row="3" Text="{DynamicResource Password}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,7"/>
                <PasswordBox x:Name="PasswordInput" Grid.Row="4" Height="46" Padding="13,0" FontSize="21" VerticalContentAlignment="Center" Background="#FFFFFF" BorderBrush="#C8D5EE" BorderThickness="1" KeyDown="Input_KeyDown"/>
                <CheckBox x:Name="RememberPasswordCheckBox" Grid.Row="5" Content="{DynamicResource RememberPassword}" FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,10,0,12"/>
                <Button x:Name="LoginButton" Grid.Row="6" Content="{DynamicResource SignIn}" Style="{StaticResource LoginButtonStyle}" IsDefault="True" Click="LoginButton_Click"/>
                <Button x:Name="LocalModeButton" Grid.Row="7" Content="{DynamicResource LocalScanningMode}" Style="{StaticResource LoginButtonStyle}" Foreground="{StaticResource PrimaryBrush}" Background="#EEF3FF" BorderBrush="#AFC2F5" Margin="0,10,0,0" Click="LocalModeButton_Click"/>
                <TextBlock x:Name="StatusTextBlock" Grid.Row="8" FontSize="18" Foreground="#C43D4B" TextWrapping="Wrap" TextAlignment="Center" Margin="0,13,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
